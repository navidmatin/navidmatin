import Layout from "@/components/layout";

# Sample MDX Page for possible blog posts

## Sample h2

### Sample H3

Can use `markdown` on this page to allow easier blogging.

```
  const test = 2 + 4;
```

bullet points:

- Test1
- Test2

export default ({ children }) => <Layout>{children}</Layout>;
